class Calculator {
    public int total;

    public void add(int x, int y) {
        total = x + y;
    }
};

void main() {
    Calculator calc = new Calculator();
    calc.add(5, 10);

    int arr[3];
    arr[0] = calc.total;
    arr[1] = arr[0] * 2;
    arr[2] = arr[1] - arr[0];
}
